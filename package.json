{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "prod": "npm run build",
        "postinstall": "npm run build && php artisan filament:assets && php artisan storage:link && php artisan config:clear && php artisan cache:clear && php artisan optimize:clear"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
